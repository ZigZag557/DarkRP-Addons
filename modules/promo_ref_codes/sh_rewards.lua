RefRewards = 
{
	money = "1000",  -- The amount per ref, both players win.
	time = "360" -- The minutes, players play time increases.
}

DarkRP.declareChatCommand({
    command = "createpromo",
    description = "Create a promo code (Superadmin only).",
    delay = 1.5
})

DarkRP.declareChatCommand({
    command = "activatepromo",
    description = "Activate a promo code.",
    delay = 1.5
})

DarkRP.declareChatCommand({
    command = "activateref",
    description = "Use a referral code.",
    delay = 1.5
})

DarkRP.declareChatCommand({
    command = "promomenu",
    description = "Open up the promo-referral menu.",
    delay = 1.5
})

DarkRP.declareChatCommand({
    command = "specialpromo",
    description = "Create a special promo code (Superadmin only!)",
    delay = 1.5
})





